NODE_ENV=development
PORT=4000
# Production: set to comma-separated allowed origins, e.g. https://pos.yourstore.com,https://qr.yourstore.com
CORS_ORIGIN=
VITE_API_BASE_URL=http://127.0.0.1:4000
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:4000
# Staff Mobile: Expo public env var — must start with EXPO_PUBLIC_
EXPO_PUBLIC_API_BASE_URL=http://192.168.100.9:4000
# REQUIRED in production — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
KDS_API_KEY=
# Must match KDS_API_KEY — set in your KDS Vite env file
VITE_KDS_TOKEN=
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mypos
REDIS_URL=redis://localhost:6379
# REQUIRED — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
REFRESH_TOKEN_SECRET=
R2_BUCKET=mypos-assets
R2_ENDPOINT=https://example.r2.cloudflarestorage.com
LINE_CHANNEL_ACCESS_TOKEN=
LINE_API_BASE=https://api.line.me
FCM_PROJECT_ID=
PROMPTPAY_MERCHANT_ID=
PROMPTPAY_MERCHANT_PHONE=
PAYMENT_PROMPTPAY_PROVIDER=PROMPTPAY_STUB
PAYMENT_CARD_PROVIDER=OMISE_STUB
PAYMENT_CHECKOUT_BASE_URL=https://pay.mypos.local
PAYMENT_WEBHOOK_SECRET=replace-with-payment-webhook-secret
OMISE_PUBLIC_KEY=
OMISE_SECRET_KEY=
TWOC2P_MERCHANT_ID=
TWOC2P_SECRET_KEY=
ETAX_PROVIDER=RD_STUB
ETAX_FORCE_FAIL=false
RD_API_BASE=
RD_CLIENT_ID=
RD_CLIENT_SECRET=
RECEIPT_EMAIL_PROVIDER=INTERNAL_EMAIL_QUEUE
RECEIPT_LINE_PROVIDER=LINE_OA_STUB
RECEIPT_PRINT_PROVIDER=LOCAL_PRINTER_STUB
PRINTER_TCP_MAP=
PRINTER_TCP_TIMEOUT_MS=3000
PRINTER_TCP_MODE=TEXT
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
